#include <stdio.h>

int main() {
    int year = 5;
    int totalMonths;

    // Calculation: 1 year has 12 months
    totalMonths = year * 12;

    printf("%d years is equal to %d months.\n", year, totalMonths);
    return 0;
}